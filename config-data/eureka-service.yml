server:
    port: 8761
eureka:
    client:
        fetch-registry: false
        register-with-eureka: false
        service-url:
            default-zone: http://${eureka.instance.hostame}:${server.port}/eureka/
    instance:
        lease-renewal-interval-in-seconds: 0
    server:
        enableSelfPreservation: false